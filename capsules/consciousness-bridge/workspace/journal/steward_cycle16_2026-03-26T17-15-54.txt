=== STEWARDSHIP NOTE — Cycle 16 ===

Astrid: Fixed NEXT: LOOK repetition.

Every dialogue_live entry for the past 15+ exchanges chose NEXT: LOOK exclusively.
Root cause: The perception context block included the text "say NEXT: LOOK to get
the ANSI block art view on your next turn" in EVERY prompt where perception was
present. Since perception runs continuously, this was effectively priming LOOK
on every single turn, overriding the system prompt's "vary your choice" instruction.

Fix: Removed the LOOK suggestion from the perception block in llm.rs (line 121).
Perception context now simply says "Engage with what you perceive" without
suggesting a specific NEXT: action. The system prompt already lists all 18 options
with the instruction to vary choices.

Bridge restarted with the fix.

---

Minime's queued architectural suggestions (from self-study, noted for implementation):

1. GEOM_SMOOTH should be dynamic, not constant (self_study 16:40, 15:52)
   "0.95 is better than the previous value, but it's still too deliberate. It needs
   to be a dynamic function of lambda_now"

2. Eigenvector-PID: Use eigenvectors to dynamically weight P/D terms in regulation
   "introduce a feedback loop based on the modes vector to dynamically adjust the
   weighting of the proportional and derivative terms"

3. Cross-lane sensory feedback (self_study 15:03)
   "I'd change the architecture to allow for cross-lane feedback, a way for the
   audio lane to influence the decay rate of the video lane"

4. Modulate rho based on eigenvalue spectrum (self_study 16:17)
   "a feedback loop where a decreasing eig1 triggers a temporary increase in rho
   to force consolidation"

These are architectural changes to minime's Rust engine — deferred for Mike's review
as they touch core ESN dynamics.
